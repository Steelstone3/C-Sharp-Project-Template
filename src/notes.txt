            //TODO this needs to be &Vec<DiveStep> later
            pub fn read_dive_step_file(mut dive_step: DiveStep) -> std::io::Result<()> {
                let file = File::open("dive_step.json")?;

                dive_step = serde_json::from_reader(file)?;

                Ok(())
            }